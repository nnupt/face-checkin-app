// js/config.js

// 1. กำหนดค่าความแม่นยำ (Threshold)
// ค่าที่ต่ำกว่าหมายถึงการจับคู่ที่เข้มงวดกว่า
const FACE_MATCH_THRESHOLD = 0.6; 

// 2. กำหนด Path ของโฟลเดอร์เก็บโมเดล
const MODELS_URI = '/face-checkin-app/models';

// 3. กำหนดชื่อผู้ใช้และโฟลเดอร์ภาพตัวอย่าง
// ชื่อโฟลเดอร์ต้องตรงกับชื่อ Label
const USER_LABELS = [
    '14810'
    // เพิ่มชื่อผู้ใช้ทั้งหมดที่ต้องการเทรนที่นี่
];
